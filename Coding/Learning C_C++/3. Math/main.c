#include <stdio.h>
#include <math.h>                       // enables math functions (sqrt, pow, etc.)

int main(){

    int x = 4;

    // x = sqrt(x);
    //x = pow(x, 4);
    

    printf("%d", x);

    return 0;
}